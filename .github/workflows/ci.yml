name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    name: check (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            smoke: xvfb-run -a pnpm smoke:electron
          - os: macos-latest
            smoke: pnpm smoke:electron
          - os: windows-latest
            smoke: pnpm smoke:electron
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    env:
      CI: true
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
      - run: ${{ matrix.smoke }}
